<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/stylesheets/style.css">
  <title>Express Candies</title>
</head>
<body>
  <h1>Candies as of <%= time %></h1>
  <ul>
    <% candies.forEach(candy => { %>
      <li>
        <form action="/candies/<%= candy._id %>?_method=DELETE" method="POST">
          <button type="submit">🗑️</button>
        </form>
        <a href="/candies/<%= candy._id %>"><%= candy.text %></a>
        - 
        <%= candy.inPantry ? 'in pantry' : 'not in pantry' %>
      </li>
    <% }) %>
  </ul>
  <a href="/candies/new">Add New Candy 🍭</a>
</body>
</html>